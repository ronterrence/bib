Always refer to `PRD.md` as the single source of truth for:
- Data models and inheritance (`catalogue`, `adherents`, `circulation`, `relances`).
- Business rules (max 5 loans, reference-only restrictions, CD-ROM deposits, immutability).
- The tech stack: Django Templates + Bootstrap 5 + Django Admin (no SPA/React).
Never write code that violates the acceptance criteria or test cases in Section 7 & 8 of PRD.md.